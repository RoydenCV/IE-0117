// Apartado del encabezados de las funciones 

// En este caso se utilizan numeros enteros ya que, es el tipo de numero el cual 
// esta asignado a los carnet estudiantiles. 
// Cabe mencionar que los numeros deben ser sin signo debido a que el formato 
// en numero binario cambia completamente, ademas no es lo que nos piden
// en este laboratorio 

// Declaración de la función bitwise_and
void print_binary(unsigned int num);
int bitwise_and(unsigned int a, unsigned int b);
int bitwise_or(unsigned int a, unsigned int b);
int bitwise_xor(unsigned int a, unsigned int b);
int bitwise_not(unsigned int a);

